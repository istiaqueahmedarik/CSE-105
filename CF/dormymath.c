#include <stdio.h>
int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int l;
        scanf("%d", &l);
        int arr[l];
        for (int i = 0; i < l; i++)
        {
            scanf("%d", &arr[i]);
        }
        sorting(arr, l);
        int left = 0;
        int right = 1;
        int ans = l;
        for (int i = 0; right <= l; i++)
        {
            if
        }
    }
}