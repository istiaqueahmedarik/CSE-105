#include <stdio.h>

int main()
{
    int b, t;
    scanf("%d%d", &b, &t);
    int arr[b];
    for (int i = 0; i < b; i++)
        scanf("%d", &arr[i]);
    int temp = 0;
    int impos = 0;
    for (int i = 0; i < b; i++)
    {
        temp +=
    }
}
